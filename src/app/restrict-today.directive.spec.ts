import { RestrictTodayDirective } from './restrict-today.directive';

describe('RestrictTodayDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictTodayDirective();
    expect(directive).toBeTruthy();
  });
});
